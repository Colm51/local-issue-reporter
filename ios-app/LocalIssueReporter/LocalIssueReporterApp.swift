import SwiftUI

// @main marks this as the first code that runs when the app launches.
@main
struct LocalIssueReporterApp: App {
    var body: some Scene {
        // WindowGroup creates the app's main window and loads the first SwiftUI screen.
        WindowGroup {
            ContentView()
        }
    }
}
