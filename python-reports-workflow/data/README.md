# Local GIS Data

Place optional local GIS datasets here when running the mapping workflow.

The mapping script looks here by default for:

```text
cycling-network-4326.gpkg
```

That GeoPackage is not included in this repository. Use a cycling network, bike lane, or multi-use trail dataset that you have permission to use.


